<%= content_for(:title, 'Create Player') %>
<h1>Create new player for match</h1>

<% if flash[:errors] %>
  <% flash[:errors].each do |error| %>
    <p><%= error %></p>
  <% end %>
<% end %>

<%= render partial: "players/form", locals: { method: 'post', url: players_path } %>


<!-- <form action="/players" method="post">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <p>
    <label>First Name</label>
    <input type="text" name="player[first_name]">
  </p>

  <p>
    <label>Last Name</label>
    <input type="text" name="player[last_name]">
  </p>
  <input type="submit" value="Create New Player">
</form>
 -->
<%= link_to 'Go Back', players_path %>